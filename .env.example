# Google AI Studio -> https://aistudio.google.com/apikey (no credit card, Flash-family models free)
GEMINI_API_KEY=your_gemini_api_key_here

# Groq Console -> https://console.groq.com/keys (no credit card)
# Used ONLY for the LLM-judge, deliberately a different model family than the generator
# to avoid a model grading its own homework.
GROQ_API_KEY=your_groq_api_key_here
